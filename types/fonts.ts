import {FontSource} from 'expo-font'

export const fonts: Record<string, FontSource> = {
  // eslint-disable-next-line @typescript-eslint/no-require-imports
  Eurovision: require('../assets/font/Eurovision.ttf'),
}